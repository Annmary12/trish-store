# import * from './generated/prisma.graphql'

type Mutation {
  createItem(
    title: String,
    description: String,
    prince: Int,
    image: String,
    largeImage: String ) : Item!
}

type Query {
  items: [Item]!
}
